# ===========================================
# tg2emall 环境配置
# ===========================================
# 注意：数据库、端口等配置已在 docker-compose.yml 中硬编码
# 这里只需要配置业务相关的参数

# Telegram API 配置（必需）
# 从 https://my.telegram.org 获取
API_ID=your_api_id_here
API_HASH=your_api_hash_here
PHONE_NUMBER=+1234567890

# Telegram 验证配置（可选）
TELEGRAM_VERIFICATION_TIMEOUT=600

# tgState 图片上传服务配置（可选）
# 如果不配置，系统会使用本地图片存储
TGSTATE_TOKEN=your_bot_token_here
TGSTATE_TARGET=@your_channel_name
TGSTATE_PASS=your_password_here
TGSTATE_MODE=p
TGSTATE_URL=http://your-domain.com:8088

# 公网访问地址（重要：用于图片上传返回的URL）
# 请将此地址修改为您的实际公网域名或IP地址
PUBLIC_URL=http://your-domain.com:8088

# ===========================================
# 以下配置已在 docker-compose.yml 中硬编码，无需修改
# ===========================================
# 数据库配置：
#   MYSQL_ROOT_PASSWORD=tg2emall
#   MYSQL_DATABASE=tg2em
#   MYSQL_USER=tg2emall
#   MYSQL_PASSWORD=tg2emall
#
# 服务端口：
#   前端服务: 8000
#   采集管理: 2003
#   采集服务: 5002
#   tgState管理: 8001
#   tgState上传: 8002
#   tgState页面: 8088
#
# 时区: Asia/Shanghai
# ===========================================
